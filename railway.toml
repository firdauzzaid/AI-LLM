[build]
builder = "nixpacks"
buildCommand = "npm install --ignore-scripts && npx turbo run build --filter=@repo/api"

[deploy]
startCommand = "cd apps/api && npx prisma migrate deploy && node dist/main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
